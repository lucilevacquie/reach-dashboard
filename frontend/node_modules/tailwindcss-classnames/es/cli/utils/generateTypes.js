export function generateTypes(classnames, prefix) {
    return ('\n  | ' +
        classnames
            .flatMap(function (classname) {
            var classnamesThatShouldKeepTheDefaultSuffix = ['cursor'];
            return classnamesThatShouldKeepTheDefaultSuffix.map(function (x) {
                var shouldKeepDefaultSuffix = classname.includes(x);
                var name = shouldKeepDefaultSuffix ? classname : classname.replace('-default', '');
                return prefix ? "'" + prefix + name + "'" : "'" + name + "'";
            });
        })
            .join('\n  | '));
}
//# sourceMappingURL=generateTypes.js.map