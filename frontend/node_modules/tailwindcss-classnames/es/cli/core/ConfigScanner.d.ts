import { TTailwindCSSConfig, TConfigVariants, TConfigFuture } from '../lib/types';
import { TThemeItems } from '../lib/types';
export declare class ConfigScanner {
    private readonly future;
    private readonly prefix;
    private readonly separator;
    private themeConfig;
    private readonly variantsConfig;
    constructor(tailwindConfig: TTailwindCSSConfig);
    getPrefix: () => string;
    getSeparator: () => string;
    getDeprecations: () => TConfigFuture;
    getTheme: () => TThemeItems;
    getVariants: () => TConfigVariants;
    getThemeProperty: (themeProperty: keyof TThemeItems) => [string[], Array<string | Record<string, string>>];
}
