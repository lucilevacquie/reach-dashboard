import { IGenerator } from './IGenerator';
declare type ClassesGroup = {
    [key: string]: string[];
};
export declare class ClassesGroupTemplateGenerator implements IGenerator {
    private readonly group;
    private readonly groupName;
    private readonly configPrefix;
    private members;
    constructor(group: ClassesGroup, groupName: string, configPrefix: string);
    generate: () => string;
    private generateMembersStatements;
    private generateGroupStatement;
}
export {};
